using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Collections;
using System.Threading.Tasks;

namespace Array
{
    /**internal class Impcoun_hashtable
    {
        static void Main(string[] args)
        {
            Queue que = new Queue();
            que.Enqueue(1);
            que.Enqueue(2); 
            que.Enqueue(3);
            que.Enqueue(4);
            foreach (var item in que)
            {
                Console.WriteLine(item);    
            }
            Hashtable hashtable= new Hashtable();
            hashtable.Add("1", "delhi");
            hashtable.Add("2", "Afkrika");
            hashtable.Add("3", "india");
            hashtable.Add("4", "panjab");

            foreach(DictionaryEntry item in que)
            {
                Console.WriteLine( $"{item.Key}->{item.Value}");
            }

        }

    }**/
}
